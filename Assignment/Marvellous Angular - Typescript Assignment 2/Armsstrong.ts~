function ChkArmstrong(num:number)
{
 do
 {
 var rem=num%10;
 var sum=sum+rem*rem*rem;
 num=num/10;
 
 }while(num>0);
if(sum==num)
return true;
else
return false;
}

var res=ChkArmstrong(153);

if(res==true)
console.log("It is Armstrong number");

